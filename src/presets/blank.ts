import { OpticalComponent } from '../physics/Component';

/**
 * Blank — empty scene template.
 * Start from scratch by adding components from the palette.
 */
export function createBlankScene(): OpticalComponent[] {
    return [];
}
